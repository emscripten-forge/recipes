context:
  name: rustworkx
  version: 0.18.1

package:
  name: ${{ name }}
  version: ${{ version }}

source:
- url: https://github.com/Qiskit/rustworkx/archive/refs/tags/${{ version }}.tar.gz
  sha256: 901e3161100834877c84b3a301da658c1153399d478667b1a0bbb9c2190d6d4f

build:
  number: 0

  files:
    exclude:
    - '**/*.pyi'
    - '**/__pycache__/**'
    - '**/*.pyc'
    - '**/test_*.py'
  python:
    skip_pyc_compilation:
    - '**/*.py'
requirements:
  build:
  - cross-python_${{ target_platform }}
  - ${{ compiler("c") }}
  - cffi
  - setuptools-rust
  - rust-nightly
  - maturin
  host:
  - python
  - openssl
  - cffi
  run:
  - cffi

tests:
- script: pytester
  requirements:
    build:
    - pytester
    run:
    - pytester-run
  files:
    recipe:
    - test_rustworkx.py

about:
  homepage: https://www.rustworkx.org/
  repository: https://github.com/Qiskit/rustworkx
  summary: A python graph library implemented in Rust
  license: Apache-2.0
  license_file: LICENSE
