--- a/cmake/sparrow-ipcConfig.cmake.in
+++ b/cmake/sparrow-ipcConfig.cmake.in
@@ -18,6 +18,17 @@ endif()
 
 find_dependency(sparrow)
 find_dependency(FlatBuffers)
+
+# Find lz4 and create alias if needed
+find_library(LZ4_LIBRARY NAMES lz4 liblz4)
+if(LZ4_LIBRARY AND NOT TARGET lz4::lz4)
+    add_library(lz4::lz4 UNKNOWN IMPORTED)
+    set_target_properties(lz4::lz4 PROPERTIES IMPORTED_LOCATION "${LZ4_LIBRARY}")
+endif()
+
+# zstd is also a dependency
+find_dependency(zstd)
+
 
 if(NOT TARGET sparrow-ipc::sparrow-ipc)
     include("${CMAKE_CURRENT_LIST_DIR}/@PROJECT_NAME@Targets.cmake")
