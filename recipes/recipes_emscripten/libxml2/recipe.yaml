context:
  version: '2.10.1'


package:
  name: libxml2
  version: '{{ version }}'

source:
  url: https://gitlab.gnome.org/GNOME/libxml2/-/archive/v{{ version }}/libxml2-v{{ version }}.tar.gz
  sha256: c89e5d4eb873f209bace159fc1c76e40ce89f4dc94ac382a6c17032624e2840c
  patches:
    - 0004-CVE-2017-8872.patch

build:
  number: 0

requirements:
  build:
    - '{{ compiler("c") }}'
    - libtool
    - autoconf
    - automake
    - libtool
    - pkg-config
  run:
    - zlib
    - libiconv
