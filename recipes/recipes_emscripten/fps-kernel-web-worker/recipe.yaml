context:
  name: fps-kernel-web-worker
  version: 0.1.7

package:
  name: ${{ name }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/fps_kernel_web_worker-${{ version }}.tar.gz
  sha256: 37daf2ca0fd225270c8d47a03b86622d387b7a5a9d25228fcc1b1756206ff2fe

build:
  number: 0
  script: ${{ PYTHON }} -m pip install . ${{ PIP_ARGS }}

requirements:
  build:
  - pip != 25.1
  - cross-python_${{ target_platform }}
  - uv
  host:
  - python
  run:
  - python
  - jupyverse-api >=0.11.0,<0.12.0
  - anyio
  - pyjs >=3

tests:
- script: pytester
  requirements:
    build:
    - pytester
    run:
    - pytester-run
  files:
    recipe:
    - test_import_kernelwebworker.py

about:
  homepage: https://github.com/jupyter-server/jupyverse/tree/main/plugins/kernel_web_worker
  license: BSD-3-Clause
  license_file: COPYING.md
  summary: An FPS plugin for the kernel web worker API

extra:
  recipe-maintainers:
  - davidbrochart
