context:
  name: propcache
  version: 0.5.3

package:
  name: ${{ name }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/${{ name }}-${{ version }}.tar.gz
  sha256: 5f283fde8fd8b1944fd1a1bd11db759b55c5f190795668d5187d3e3b1165d98b

build:
  number: 0
  script: ${{ PYTHON }} -m pip install . ${{ PIP_ARGS }}

  files:
    exclude:
    - '**/*.pyx'
    - '**/__pycache__/**'
    - '**/*.pyc'
    - '**/test_*.py'
  python:
    skip_pyc_compilation:
    - '**/*.py'
requirements:
  build:
  - ${{ compiler('cxx')}}
  - cross-python_emscripten-wasm32
  - python
  - cython
  - expandvars
  host:
  - python

tests:
- script: pytester
  requirements:
    build:
    - pytester
    run:
    - pytester-run
  files:
    recipe:
    - test_propcache.py

about:
  homepage: https://github.com/aio-libs/propcache
  license: Apache-2.0
  license_family: Apache
  license_file: LICENSE
  summary: Accelerated property cache
  documentation: https://github.com/aio-libs/propcache
  repository: https://github.com/aio-libs/propcache

extra:
  maintainers:
  - IsabelParedes
