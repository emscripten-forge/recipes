context:
  version: 1.12.1
  filename: hdf5-${{ version | replace('.', '_') }}.tar.gz

package:
  name: libhdf5
  version: ${{ version }}

source:
  url: https://github.com/HDFGroup/hdf5/archive/refs/tags/${{ filename }}
  sha256: e6dde173c2d243551922d23a0387a79961205b018502e6a742acb30b61bc2d5f
  patches:
    - patches/0001-Hardcode-float-precision.patch

build:
  number: 0

requirements:
  build:
    - zlib
    - cmake
    - ninja
    - ${{ compiler("c") }}
  host:
    - zlib

