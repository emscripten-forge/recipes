context:
  version: 10.8.10

package:
  name: passagemath-topcom
  version: ${{ version }}

source:
- url: https://pypi.org/packages/source/p/passagemath-topcom/passagemath_topcom-${{ version }}.tar.gz
  sha256: 14159b0f20f4e114a8ecc6932d96a920c05e8366b1c36a58313d89e251de71c9

build:
  script: ${{ PYTHON }} -m pip install .
  number: 0

requirements:
  build:
  - python
  - crossenv >=1.2
  - cross-python_emscripten-wasm32
  - ${{ compiler("c") }}
  - ${{ compiler("cxx") }}
  - pip
  - passagemath-setup
  - passagemath-environment
  - cython<3.3
  - cysignals
  - pkgconfig
  - passagemath-abi == ${{ version }}
  host:
  - python
  - topcom
  - passagemath-categories
  run:
  - python
  - topcom
  - passagemath-categories
  - passagemath-environment
  - passagemath-polyhedra
  - pexpect

tests:
- script: pytester
  files:
    recipe:
    - test_passagemath_topcom.py
  requirements:
    build:
    - pytester
    run:
    - pytester-run
    - passagemath-repl

about:
  license: GPL-2.0-or-later
  license_file: README.rst
  summary: 'passagemath: Triangulations of point configurations and oriented matroids with TOPCOM'
  description: |
    Distribution of a part of the Sage library.
    It provides an interface to TOPCOM, a package for computing triangulations
    of point configurations and oriented matroids.

extra:
  emscripten_tests:
    python:
      pytest_files:
      - test_passagemath_topcom.py
  recipe-maintainers:
  - mkoeppe
