context:
  version: 10.8.10

package:
  name: passagemath-iml
  version: ${{ version }}

source:
- url: https://pypi.org/packages/source/p/passagemath-iml/passagemath_iml-${{ version }}.tar.gz
  sha256: a706e7ec8c6feeba766c5a0995a9eab2af710ece13bae2b9788a1b8bc05bf692

build:
  script: |
    export LDFLAGS="$LDFLAGS -Wl,--allow-multiple-definition"
    ${{ PYTHON }} -m pip install .
  number: 0

requirements:
  build:
  - python
  - crossenv >=1.2
  - cross-python_emscripten-wasm32
  - ${{ compiler("c") }}
  - ${{ compiler("cxx") }}
  - pip
  - passagemath-setup
  - passagemath-environment
  - cython<3.3
  - cysignals
  - pkgconfig
  - numpy
  - passagemath-abi == ${{ version }}
  host:
  - python
  - gmp
  - mpfr
  - mpc
  - libflint
  - iml
  - numpy
  - cysignals
  - memory-allocator
  - mpmath
  - passagemath-categories
  - passagemath-flint
  - passagemath-modules
  run:
  - python
  - conway-polynomials
  - cysignals
  - memory-allocator
  - passagemath-categories
  - passagemath-environment
  - passagemath-flint
  - passagemath-modules

tests:
- script: pytester
  files:
    recipe:
    - test_passagemath_iml.py
  requirements:
    build:
    - pytester
    run:
    - pytester-run
    - passagemath-modules
    - passagemath-repl

about:
  license: GPL-2.0-or-later
  license_file: README.rst
  summary: 'passagemath: Linear Algebra with IML'
  description: |
    Distribution of a part of the Sage library.
    It provides modules that depend on the library IML.

extra:
  emscripten_tests:
    python:
      pytest_files:
      - test_passagemath_iml.py
  recipe-maintainers:
  - mkoeppe
