context:
  name: simplejson
  version: 3.20.2

package:
  name: ${{ name }}
  version: ${{ version }}

source:
- url: https://pypi.io/packages/source/${{ name[0] }}/${{ name }}/${{ name }}-${{ version }}.tar.gz
  sha256: 5fe7a6ce14d1c300d80d08695b7f7e633de6cd72c80644021874d985b3393649

build:
  number: 0
  script: ${PYTHON} -m pip install . ${PIP_ARGS}

requirements:
  build:
  - ${{ compiler("cxx") }}
  - cross-python_${{ target_platform }}
  - python
  - cython
  - numpy
  - pip
  - extension-helpers
  host:
  - numpy
  - python

tests:
- script: pytester
  requirements:
    build:
    - pytester
    run:
    - pytester-run
  files:
    recipe:
    - test_simplejson.py

about:
  homepage: http://github.com/simplejson/simplejson
  license: MIT
  license_family: MIT
  license_file: LICENSE.txt
  summary: Simple, fast, extensible JSON encoder/decoder for Python
  description: |
    Simplejson is a simple, fast, complete, correct and extensible JSON
    <http://json.org> encoder and decoder for Python 2.5+ and Python 3.3+. It is
    pure Python code with no dependencies, but includes an optional C extension
    for a serious speed boost.

extra:
  recipe-maintainers:
  - DerThorsten
