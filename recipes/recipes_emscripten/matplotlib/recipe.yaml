package:
  name: matplotlib
  version: 3.5.1

build:
  number: 2

requirements:
  run:
    - {{ pin_subpackage('matplotlib-base', max_pin="x.x.x") }}

extra:
  emscripten_tests:
    python:
      pytest_files:
      - test_import_matplotlib.py
