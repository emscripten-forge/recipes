diff --git a/numba/np/ufunc/decorators.py b/numba/np/ufunc/decorators.py
--- a/numba/np/ufunc/decorators.py
+++ b/numba/np/ufunc/decorators.py
@@ -1,4 +1,5 @@
 import inspect
+import sys
 
 from numba.np.ufunc import _internal
 from numba.np.ufunc.parallel import ParallelUFuncBuilder, ParallelGUFuncBuilder
@@ -14,7 +15,10 @@ class _BaseVectorize(object):
 
     @classmethod
     def get_cache(cls, kwargs):
-        return kwargs.pop('cache', False)
+        cache = kwargs.pop('cache', False)
+        if sys.platform == "emscripten":
+            return False
+        return cache
 
     @classmethod
     def get_writable_args(cls, kwargs):
@@ -25,6 +29,12 @@ class _BaseVectorize(object):
     @classmethod
     def get_target_implementation(cls, kwargs):
         target = kwargs.pop('target', 'cpu')
+        # The parallel ufunc target initializes multiprocessing locks and a
+        # native threading backend, neither of which exists in the current
+        # single-threaded Emscripten runtime.  Preserve the API and execute the
+        # same kernel through the serial CPU target instead.
+        if sys.platform == "emscripten" and target == "parallel":
+            target = "cpu"
         try:
             return cls.target_registry[target]
         except KeyError:
