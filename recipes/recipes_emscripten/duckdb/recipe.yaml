context:
  name: duckdb
  version: 1.4.4

package:
  name: ${{ name }}
  version: ${{ version }}

source:
  url: https://github.com/duckdb/duckdb/archive/refs/tags/v${{ version }}.tar.gz
  sha256: 43645e15419c6539bae6915ba397de6569e4a7ca0d502be95d653a78fdb0bece

build:
  number: 0
  script: build.sh

  files:
    exclude:
    - '**.dist-info/**'
    - '**/__pycache__/**'
    - '**/*.pyc'
    - '**/test_*.py'
    - 'duckdb_build/**'
  python:
    skip_pyc_compilation:
    - '**/*.py'

requirements:
  build:
  - python
  - cross-python_${{ target_platform }}
  - ${{ compiler('cxx') }}
  - pip
  - setuptools
  - pybind11
  - scikit-build-core
  - cmake
  - ninja
  host:
  - python
  - pybind11
  run:
  - python

tests:
- script: pytester
  files:
    recipe:
    - test_duckdb.py
  requirements:
    build:
    - pytester
    run:
    - pytester-run

about:
  homepage: https://duckdb.org/
  license: MIT
  license_file: LICENSE
  summary: DuckDB is an analytical in-process SQL database management system
  description: |
    DuckDB is an in-process SQL OLAP database management system.
    It is designed to support analytical query workloads (OLAP) while
    being embeddable in Python and other languages.
  repository: https://github.com/duckdb/duckdb

extra:
  recipe-maintainers:
  - wolfv
