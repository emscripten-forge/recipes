context:
  version: 10.8.6

package:
  name: passagemath-bliss
  version: ${{ version }}

source:
- url: https://pypi.org/packages/source/p/passagemath-bliss/passagemath_bliss-${{ version }}.tar.gz
  sha256: 9e0e909ebd4920c1c8ea0c6405b962155061d126e1c4d12ee4e23c808311568d

build:
  script: ${{ PYTHON }} -m pip install .
  number: 0

requirements:
  build:
  - python
  - crossenv >=1.2
  - cross-python_emscripten-wasm32
  - ${{ compiler("c") }}
  - ${{ compiler("cxx") }}
  - pip
  - passagemath-setup
  - passagemath-environment
  - cython
  - cysignals
  - pkgconfig
  - passagemath-abi == ${{ version }}
  host:
  - python
  - bliss
  run:
  - python
  - cysignals

tests:
- script: pytester
  files:
    recipe:
    - test_passagemath_bliss.py
  requirements:
    build:
    - pytester
    run:
    - pytester-run
    - passagemath-graphs
    - passagemath-repl

about:
  license: GPL-2.0-or-later
  license_file: README.rst
  summary: 'passagemath: Graph (iso/auto)morphisms with bliss'
  description: |
    Distribution of a part of the Sage library.
    It provides a Cython interface to the bliss library for the purpose
    of computing graph (iso/auto)morphisms.

extra:
  emscripten_tests:
    python:
      pytest_files:
      - test_passagemath_bliss.py
  recipe-maintainers:
  - mkoeppe
