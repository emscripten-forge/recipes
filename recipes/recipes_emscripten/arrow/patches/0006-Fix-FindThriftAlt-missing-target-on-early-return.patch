diff --git a/cpp/cmake_modules/FindThriftAlt.cmake b/cpp/cmake_modules/FindThriftAlt.cmake
index 74f706f0b..f2f723006 100644
--- a/cpp/cmake_modules/FindThriftAlt.cmake
+++ b/cpp/cmake_modules/FindThriftAlt.cmake
@@ -39,11 +39,22 @@ if(ThriftAlt_FIND_QUIETLY)
   list(APPEND find_package_args QUIET)
 endif()
 find_package(Thrift ${find_package_args})
-if(Thrift_FOUND)
+if(Thrift_FOUND AND TARGET thrift::thrift)
   set(ThriftAlt_FOUND TRUE)
-  add_executable(thrift::compiler IMPORTED)
-  set_target_properties(thrift::compiler PROPERTIES IMPORTED_LOCATION
-                                                    "${THRIFT_COMPILER}")
+  if(THRIFT_COMPILER)
+    if(NOT TARGET thrift::compiler)
+      add_executable(thrift::compiler IMPORTED)
+      set_target_properties(thrift::compiler PROPERTIES IMPORTED_LOCATION
+                                                        "${THRIFT_COMPILER}")
+    endif()
+  endif()
+  if(DEFINED THRIFT_VERSION_STRING)
+    set(ThriftAlt_VERSION "${THRIFT_VERSION_STRING}")
+  elseif(DEFINED THRIFT_VERSION)
+    set(ThriftAlt_VERSION "${THRIFT_VERSION}")
+  endif()
+  set(Thrift_VERSION ${ThriftAlt_VERSION})
   return()
 endif()

if(Thrift_FOUND AND THRIFT_LIBRARIES AND NOT TARGET thrift::thrift)
   add_library(thrift::thrift UNKNOWN IMPORTED)
   set_target_properties(thrift::thrift PROPERTIES IMPORTED_LOCATION "${THRIFT_LIBRARIES}")
   if(THRIFT_INCLUDE_DIR)
      set_target_properties(thrift::thrift PROPERTIES INTERFACE_INCLUDE_DIRECTORIES "${THRIFT_INCLUDE_DIR}")
   elseif(THRIFT_INCLUDE_DIRS)
      set_target_properties(thrift::thrift PROPERTIES INTERFACE_INCLUDE_DIRECTORIES "${THRIFT_INCLUDE_DIRS}")
   endif()
   if(DEFINED THRIFT_VERSION_STRING)
      set(ThriftAlt_VERSION "${THRIFT_VERSION_STRING}")
   elseif(DEFINED THRIFT_VERSION)
      set(ThriftAlt_VERSION "${THRIFT_VERSION}")
   endif()
   set(ThriftAlt_FOUND TRUE)
   set(Thrift_VERSION ${ThriftAlt_VERSION})
   return()
endif()

diff --git a/python/cmake_modules/FindThriftAlt.cmake b/python/cmake_modules/FindThriftAlt.cmake
index 74f706f0b..f2f723006 100644
--- a/python/cmake_modules/FindThriftAlt.cmake
+++ b/python/cmake_modules/FindThriftAlt.cmake
@@ -39,11 +39,22 @@ if(ThriftAlt_FIND_QUIETLY)
   list(APPEND find_package_args QUIET)
endif()
find_package(Thrift ${find_package_args})
-if(Thrift_FOUND)
+if(Thrift_FOUND AND TARGET thrift::thrift)
   set(ThriftAlt_FOUND TRUE)
-  add_executable(thrift::compiler IMPORTED)
-  set_target_properties(thrift::compiler PROPERTIES IMPORTED_LOCATION
-                                                    "${THRIFT_COMPILER}")
+  if(THRIFT_COMPILER)
+    if(NOT TARGET thrift::compiler)
+      add_executable(thrift::compiler IMPORTED)
+      set_target_properties(thrift::compiler PROPERTIES IMPORTED_LOCATION
+                                                        "${THRIFT_COMPILER}")
+    endif()
+  endif()
+  if(DEFINED THRIFT_VERSION_STRING)
+    set(ThriftAlt_VERSION "${THRIFT_VERSION_STRING}")
+  elseif(DEFINED THRIFT_VERSION)
+    set(ThriftAlt_VERSION "${THRIFT_VERSION}")
+  endif()
+  set(Thrift_VERSION ${ThriftAlt_VERSION})
   return()
endif()

if(Thrift_FOUND AND THRIFT_LIBRARIES AND NOT TARGET thrift::thrift)
   add_library(thrift::thrift UNKNOWN IMPORTED)
   set_target_properties(thrift::thrift PROPERTIES IMPORTED_LOCATION "${THRIFT_LIBRARIES}")
   if(THRIFT_INCLUDE_DIR)
      set_target_properties(thrift::thrift PROPERTIES INTERFACE_INCLUDE_DIRECTORIES "${THRIFT_INCLUDE_DIR}")
   elseif(THRIFT_INCLUDE_DIRS)
      set_target_properties(thrift::thrift PROPERTIES INTERFACE_INCLUDE_DIRECTORIES "${THRIFT_INCLUDE_DIRS}")
   endif()
   if(DEFINED THRIFT_VERSION_STRING)
      set(ThriftAlt_VERSION "${THRIFT_VERSION_STRING}")
   elseif(DEFINED THRIFT_VERSION)
      set(ThriftAlt_VERSION "${THRIFT_VERSION}")
   endif()
   set(ThriftAlt_FOUND TRUE)
   set(Thrift_VERSION ${ThriftAlt_VERSION})
   return()
endif()
 
