context:
  name: libpng
  version: 1.6.52

package:
  name: ${{ name }}
  version: ${{ version }}

source:
  url: https://downloads.sourceforge.net/project/${{ name }}/libpng16/${{ version }}/${{ name }}-${{ version }}.tar.gz
  sha256: 86d4a88be1c8bc903674199f1d067a9ac940af4e4399caba0314e7a1bcaa0724

build:
  number: 0

requirements:
  build:
  - ${{ compiler("c") }}
  - libtool
  host:
  - zlib

tests:
- package_contents:
    files:
    - bin/pngfix.wasm
    - bin/png-fix-itxt.wasm
    - lib/libpng16.a
    - include/png.h
- script:
  - libpng-config --version

about:
  license: zlib-acknowledgement
  license_file: LICENSE
  summary: PNG reference library
  description: |
    libpng is the official PNG reference library. It supports almost all PNG
    features, is extensible, and has been extensively tested for over 20 years.
  homepage: http://www.libpng.org/pub/png/libpng.html

extra:
  recipe-maintainers:
  - martinRenou
  - IsabelParedes
