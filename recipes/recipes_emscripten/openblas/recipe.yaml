context:
  version: 0.3.23
  name: openblas

package:
  name: ${{ name }}
  version: ${{ version }}

build:
  number: 2

requirements:
  build:
  - ${{ compiler('c') }}


extra:
  recipe-maintainers:
  - DerThorsten
