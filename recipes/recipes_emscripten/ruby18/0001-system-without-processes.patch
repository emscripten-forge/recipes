--- a/process.c
+++ b/process.c
@@ -1593,6 +1593,15 @@
 	rb_syswait(pid);
     }
     signal(SIGCHLD, chfunc);
+#ifdef __EMSCRIPTEN__
+    /* WebAssembly has no child processes: behave like sh does for a
+       command that cannot be found (exit status 127), so callers that
+       test the result of system() keep working. */
+    if (pid < 0 && errno == ENOSYS) {
+	last_status_set(127 << 8, 0);
+	return Qfalse;
+    }
+#endif
     if (pid < 0) rb_sys_fail(0);
     status = NUM2INT(rb_last_status);
 #endif
