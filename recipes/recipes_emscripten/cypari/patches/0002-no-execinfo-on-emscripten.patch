--- a/cypari/implementation.c
+++ b/cypari/implementation.c
@@ -44,7 +44,10 @@
 #include <sys/types.h>
 #include <sys/wait.h>
 #include <unistd.h>
+#ifndef __EMSCRIPTEN__
+/* Emscripten's sysroot has no execinfo.h: wasm has no libc backtrace. */
 #include <execinfo.h>
+#endif
 #include <Python.h>
 #include <pari/pari.h>
 #include "struct_signals.h"
@@ -282,10 +285,12 @@
 /* Print a backtrace if supported by libc */
 static void print_backtrace()
 {
-    void* backtracebuffer[1024];
     fflush(stderr);
+#ifndef __EMSCRIPTEN__
+    void* backtracebuffer[1024];
     int btsize = backtrace(backtracebuffer, 1024);
     backtrace_symbols_fd(backtracebuffer, btsize, 2);
+#endif
     print_sep();
 }
 
