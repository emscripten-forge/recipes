context:
  version: 10.8.9

package:
  name: passagemath-gsl
  version: ${{ version }}

source:
- url: https://pypi.org/packages/source/p/passagemath-gsl/passagemath_gsl-${{ version }}.tar.gz
  sha256: 69ce6fd90a3b1510061be64c41ec25aa9956ce7cb2ed58ed0cc63d222e323855

build:
  script: ${{ PYTHON }} -m pip install .
  number: 1

requirements:
  build:
  - python
  - crossenv >=1.2
  - cross-python_emscripten-wasm32
  - ${{ compiler("c") }}
  - ${{ compiler("cxx") }}
  - pip
  - passagemath-setup
  - passagemath-environment
  - cython<3.3
  - cysignals
  - numpy
  - pkgconfig
  - passagemath-abi == ${{ version }}
  host:
  - python
  - gsl
  - gmp
  - memory-allocator
  - numpy
  - passagemath-categories
  - passagemath-modules
  run:
  - python
  - cysignals
  - memory-allocator
  - numpy
  - passagemath-categories
  - passagemath-environment
  - passagemath-modules

tests:
- script: pytester
  files:
    recipe:
    - test_passagemath_gsl.py
  requirements:
    build:
    - pytester
    run:
    - pytester-run
    - passagemath-repl

about:
  license: GPL-2.0-or-later
  license_file: README.rst
  summary: 'passagemath: Numerics with the GNU Scientific Library'
  description: |
    Distribution of a part of the Sage library.
    It provides modules that depend on the GNU Scientific Library.

extra:
  emscripten_tests:
    python:
      pytest_files:
      - test_passagemath_gsl.py
  recipe-maintainers:
  - mkoeppe
