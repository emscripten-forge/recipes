context:
  version: 3.10.2

package:
  name: libpython
  version: '{{ version }}'

build:
  number: 0

requirements:
  run:
    - python
    - bzip2
    - zlib
    - libffi
    - sqlite

about:
  home: todo
  summary: we need different dependencies when we want to build things ontop of python (ie xeus-python/pyjs) compared to the situation where a pkg like pands dependes on python.
  license_file: todo

extra:
  recipe-maintainers:
    - DerThorsten
