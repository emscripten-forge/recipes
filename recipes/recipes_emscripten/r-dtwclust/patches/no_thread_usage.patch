diff --git a/DESCRIPTION b/DESCRIPTION
index 9828f27..4dbd722 100644
--- a/DESCRIPTION
+++ b/DESCRIPTION
@@ -33,15 +33,10 @@ Imports:
     Matrix (>= 1.5-0),
     RSpectra,
     Rcpp,
-    RcppParallel (>= 4.4.0),
     reshape2,
-    shiny,
-    shinyjs
 LinkingTo:
     Rcpp,
     RcppArmadillo,
-    RcppParallel,
-    RcppThread
 Suggests:
     doParallel,
     iterators,
@@ -108,4 +103,4 @@ Collate:
     'UTILS-tslist.R'
     'UTILS-utils.R'
     'UTILS-zscore.R'
-    'pkg.R'
+    'pkg.R'
\ No newline at end of file
diff --git a/NAMESPACE b/NAMESPACE
index 2ddc3d8..9445211 100644
--- a/NAMESPACE
+++ b/NAMESPACE
@@ -73,8 +73,8 @@ importFrom(Matrix,rowSums)
 importFrom(Matrix,sparseMatrix)
 importFrom(RSpectra,eigs_sym)
 importFrom(Rcpp,evalCpp)
-importFrom(RcppParallel,RcppParallelLibs)
-importFrom(RcppParallel,defaultNumThreads)
+#importFrom(RcppParallel,RcppParallelLibs)
+#importFrom(RcppParallel,defaultNumThreads)
 importFrom(clue,as.cl_class_ids)
 importFrom(clue,as.cl_membership)
 importFrom(clue,cl_class_ids)
@@ -135,9 +135,9 @@ importFrom(rlang,exprs)
 importFrom(rlang,is_call)
 importFrom(rlang,list2)
 importFrom(rlang,syms)
-importFrom(shiny,runApp)
-importFrom(shiny,shinyApp)
-importFrom(shinyjs,useShinyjs)
+#importFrom(shiny,runApp)
+#importFrom(shiny,shinyApp)
+#importFrom(shinyjs,useShinyjs)
 importFrom(stats,approx)
 importFrom(stats,as.dist)
 importFrom(stats,as.hclust)
@@ -153,4 +153,4 @@ importFrom(stats,runif)
 importFrom(stats,update)
 importFrom(utils,head)
 importFrom(utils,packageVersion)
-useDynLib(dtwclust, .registration = TRUE)
+useDynLib(dtwclust, .registration = TRUE)
\ No newline at end of file
diff --git a/R/RD-helpers.R b/R/RD-helpers.R
index d074652..672d730 100644
--- a/R/RD-helpers.R
+++ b/R/RD-helpers.R
@@ -16,7 +16,7 @@ roxygen_proxy_section <- function() {
 The version registered with [proxy::dist()] is custom (`loop = FALSE` in [proxy::pr_DB]).
 The custom function handles multi-threaded parallelization directly with [RcppParallel][RcppParallel::RcppParallel-package].
 It uses all available threads by default (see [RcppParallel::defaultNumThreads()]),
-but this can be changed by the user with [RcppParallel::setThreadOptions()].
+but this can be changed by the user with [#RcppParallel::setThreadOptions()].
 
 An exception to the above is when it is called within a [`foreach`][foreach::foreach] parallel loop **made by dtwclust**.
 If the parallel workers do not have the number of threads explicitly specified,
@@ -39,7 +39,7 @@ The overhead introduced is sometimes too large, and it's better to run tasks seq
 
 This function uses the [RcppParallel][RcppParallel::RcppParallel-package] package for parallelization.
 It uses all available threads by default (see [RcppParallel::defaultNumThreads()]),
-but this can be changed by the user with [RcppParallel::setThreadOptions()].
+but this can be changed by the user with [#RcppParallel::setThreadOptions()].
 
 An exception to the above is when it is called within a [`foreach`][foreach::foreach] parallel loop **made by dtwclust**.
 If the parallel workers do not have the number of threads explicitly specified,
diff --git a/R/SHINY-ssdtwclust.R b/R/SHINY-ssdtwclust.R
index 6c0869e..8975a9a 100644
--- a/R/SHINY-ssdtwclust.R
+++ b/R/SHINY-ssdtwclust.R
@@ -112,7 +112,7 @@
 #' workers <- makeCluster(detectCores())
 #' clusterEvalQ(workers, {
 #'     library(dtwclust)
-#'     RcppParallel::setThreadOptions(1L)
+#'     #RcppParallel::setThreadOptions(1L)
 #' })
 #' registerDoParallel(workers)
 #' ssdtwclust(reinterpolate(CharTraj[1L:20L], 150L))
diff --git a/R/UTILS-utils.R b/R/UTILS-utils.R
index 93ad802..e37c946 100644
--- a/R/UTILS-utils.R
+++ b/R/UTILS-utils.R
@@ -245,7 +245,7 @@ setnames_inplace <- function(vec, names) {
             if (nzchar(Sys.getenv("RCPP_PARALLEL_NUM_THREADS")))
                 reset <- FALSE # nocov
             else
-                RcppParallel::setThreadOptions(1L)
+                #RcppParallel::setThreadOptions(1L)
             reset
         }
     }
@@ -281,7 +281,7 @@ setnames_inplace <- function(vec, names) {
             .inorder = FALSE,
             .packages = "dtwclust"
         ) %dopar% {
-            RcppParallel::setThreadOptions("auto")
+            #RcppParallel::setThreadOptions("auto")
         }
     }
     # reset RNGkind if needed
@@ -333,10 +333,9 @@ validate_pairwise <- function(x, y) {
 }
 
 # Number of configured/available threads according to RcppParallel
-#' @importFrom RcppParallel defaultNumThreads
 #'
 get_nthreads <- function() {
-    as.integer(Sys.getenv("RCPP_PARALLEL_NUM_THREADS", RcppParallel::defaultNumThreads()))
+    as.integer(1)
 }
 
 # ==================================================================================================
diff --git a/R/pkg.R b/R/pkg.R
index 920bdcf..107a77a 100644
--- a/R/pkg.R
+++ b/R/pkg.R
@@ -63,7 +63,6 @@
 #' @useDynLib dtwclust, .registration = TRUE
 #' @import foreach
 #' @importFrom Rcpp evalCpp
-#' @importFrom RcppParallel RcppParallelLibs
 #'
 "_PACKAGE"
 
diff --git a/src/Makevars b/src/Makevars
index 7e82752..ec37aab 100644
--- a/src/Makevars
+++ b/src/Makevars
@@ -1,11 +1,4 @@
 PKG_CXXFLAGS += -DRCPP_USE_UNWIND_PROTECT
-PKG_CXXFLAGS += $(shell ${R_HOME}/bin/Rscript -e "RcppParallel::CxxFlags()")
-
-# Needed due to RcppArmadillo
-PKG_LIBS += $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)
-# Needed due to RcppParallel
-PKG_LIBS += $(shell ${R_HOME}/bin/Rscript -e "RcppParallel::RcppParallelLibs()")
-
 # Get source files (see https://stackoverflow.com/a/27794283/5793905 but note the quotes)
 CPP_SOURCES = $(shell find . -type f -name '*.cpp')
 
diff --git a/src/centroids/dba.cpp b/src/centroids/dba.cpp
index b8b0075..6cad083 100644
--- a/src/centroids/dba.cpp
+++ b/src/centroids/dba.cpp
@@ -4,7 +4,7 @@
 #include <utility> // std::move
 
 #include <RcppArmadillo.h>
-#include <RcppParallel.h>
+
 
 #include "../distances/calculators.h"
 #include "../distances/details.h" // dtw_basi
diff --git a/src/centroids/sdtw-cent.cpp b/src/centroids/sdtw-cent.cpp
index 12b8287..c873f07 100644
--- a/src/centroids/sdtw-cent.cpp
+++ b/src/centroids/sdtw-cent.cpp
@@ -5,7 +5,7 @@
 #include <utility> // std::move
 
 #include <RcppArmadillo.h>
-#include <RcppParallel.h>
+
 
 #include "../distances/calculators.h"
 #include "../distances/details.h" // sdtw
diff --git a/src/distmat/distmat-loop.cpp b/src/distmat/distmat-loop.cpp
index 4440617..ada4edc 100644
--- a/src/distmat/distmat-loop.cpp
+++ b/src/distmat/distmat-loop.cpp
@@ -2,6 +2,7 @@
 
 #include <RcppArmadillo.h>
 
+#include "../utils/ParallelWorker.h"
 #include "../distances/calculators.h"
 #include "distmat.h"
 #include "fillers.h"
diff --git a/src/distmat/distmat.cpp b/src/distmat/distmat.cpp
index 38185f3..20e555f 100644
--- a/src/distmat/distmat.cpp
+++ b/src/distmat/distmat.cpp
@@ -5,7 +5,7 @@
 #include <string>
 
 #include <Rcpp.h>
-#include <RcppParallel.h>
+
 
 #include "distmat.h"
 
diff --git a/src/distmat/distmat.h b/src/distmat/distmat.h
index 10a9533..e2fa024 100644
--- a/src/distmat/distmat.h
+++ b/src/distmat/distmat.h
@@ -3,9 +3,9 @@
 
 #include <memory> // *_ptr
 
-#include <RcppParallel.h>
-#include <Rinternals.h>
 
+#include <Rinternals.h>
+#include "../utils/ParallelWorker.h"
 #include "../utils/utils.h" // id_t
 
 namespace dtwclust {
diff --git a/src/distmat/dtw-lb.cpp b/src/distmat/dtw-lb.cpp
index 2a6e51a..fb97b76 100644
--- a/src/distmat/dtw-lb.cpp
+++ b/src/distmat/dtw-lb.cpp
@@ -4,7 +4,6 @@
 #include <memory> // shared_ptr
 
 #include <RcppArmadillo.h>
-#include <RcppParallel.h>
 
 #include "../distances/calculators.h"
 #include "../utils/ParallelWorker.h"
diff --git a/src/distmat/fillers.cpp b/src/distmat/fillers.cpp
index 0343459..83c5b12 100644
--- a/src/distmat/fillers.cpp
+++ b/src/distmat/fillers.cpp
@@ -5,7 +5,6 @@
 #include <string>
 
 #include <RcppArmadillo.h>
-#include <RcppParallel.h>
 
 #include "../utils/ParallelWorker.h"
 #include "../utils/utils.h" // get_grain, s2d, id_t
diff --git a/src/tadpole/tadpole.cpp b/src/tadpole/tadpole.cpp
index 759d179..92d8028 100644
--- a/src/tadpole/tadpole.cpp
+++ b/src/tadpole/tadpole.cpp
@@ -9,7 +9,7 @@
 #include <vector>
 
 #include <RcppArmadillo.h>
-#include <RcppParallel.h>
+
 
 #include "../distances/calculators.h"
 #include "../utils/ParallelWorker.h"
diff --git a/src/utils/ParallelWorker.cpp b/src/utils/ParallelWorker.cpp
index bf54143..c77ba05 100644
--- a/src/utils/ParallelWorker.cpp
+++ b/src/utils/ParallelWorker.cpp
@@ -2,16 +2,13 @@
 
 #include <cstddef> // std::size_t
 
-#include <RcppParallel.h>
 #undef ERROR // collision between R.h and mingw_32/i686-w64-mingw32/include/windows.h
-#include <RcppThread.h>
+
 
 namespace dtwclust {
 
 void ParallelWorker::operator()(std::size_t begin, std::size_t end) {
     work_it(begin, end);
-    // make sure this is called at least once per thread call
-    RcppThread::isInterrupted();
 }
 
 ParallelWorker::ParallelWorker(const int grain, const int min, const int max)
@@ -19,11 +16,11 @@ ParallelWorker::ParallelWorker(const int grain, const int min, const int max)
 { }
 
 bool ParallelWorker::is_interrupted() const {
-    return RcppThread::isInterrupted();
+    return false;
 }
 
 bool ParallelWorker::is_interrupted(const std::size_t i) const {
-    return RcppThread::isInterrupted(i % interrupt_grain_ == 0);
+    return false;
 }
 
 // how often to check for user interrupt inside a thread
diff --git a/src/utils/ParallelWorker.h b/src/utils/ParallelWorker.h
index 2050984..bc8a1ef 100644
--- a/src/utils/ParallelWorker.h
+++ b/src/utils/ParallelWorker.h
@@ -1,22 +1,74 @@
 #ifndef DTWCLUST_PARALLELWORKER_HPP_
 #define DTWCLUST_PARALLELWORKER_HPP_
-
+#include "R-gateways.h"
 #include <cstddef> // std::size_t
 
-#include <RcppParallel.h>
-#undef ERROR // collision between R.h and mingw_32/i686-w64-mingw32/include/windows.h
-#include <RcppThread.h>
+#include <Rcpp.h>
+
+
+namespace RcppParallel
+{
+    class Worker {
+    public:
+        virtual ~Worker() = default;
+        virtual void operator()(std::size_t begin,
+                            std::size_t end) = 0;
+        
+    };
+
+    template<typename T>
+    struct RVector;
+
+    template<>
+    struct RVector<double> : public Rcpp::NumericVector {
+        using Rcpp::NumericVector::NumericVector;
+    };
+
+    template<>
+    struct RVector<int> : public Rcpp::IntegerVector {
+        using Rcpp::IntegerVector::IntegerVector;
+    };
+
+    template<typename T>
+    struct RMatrix;
+
+    template<>
+    struct RMatrix<double> : public Rcpp::NumericMatrix {
+        using Rcpp::NumericMatrix::NumericMatrix;
+    };
+
+    template<>
+    struct RMatrix<int> : public Rcpp::IntegerMatrix {
+        using Rcpp::IntegerMatrix::IntegerMatrix;
+    };
+
+    
+}
+
 
 namespace dtwclust {
 
+
 inline void parallel_for(std::size_t begin, std::size_t end, RcppParallel::Worker& worker,
                          std::size_t grain_size = 1)
 {
-    RcppParallel::parallelFor(begin, end, worker, grain_size);
-    // always call after parallelFor to actually throw exception if there was an interruption
-    RcppThread::checkUserInterrupt();
+    grain_size = std::max<std::size_t>(grain_size, 1);
+
+    for (std::size_t i = begin; i < end; i += grain_size) {
+        std::size_t j = std::min(i + grain_size, end);
+        worker(i, j);
+    }
 }
 
+
+
+struct NoOpMutex{
+    void lock(){}
+    void unlock(){}
+};
+
+
+
 class ParallelWorker : public RcppParallel::Worker
 {
 public:
@@ -29,7 +81,7 @@ protected:
     bool is_interrupted() const;
     bool is_interrupted(const std::size_t i) const;
 
-    tthread::mutex mutex_;
+    NoOpMutex mutex_;
 
 private:
     int interrupt_grain(const int grain, const int min, const int max) const;
diff --git a/tests/testthat.R b/tests/testthat.R
index 9da4678..9e9aa06 100644
--- a/tests/testthat.R
+++ b/tests/testthat.R
@@ -3,7 +3,7 @@ library(foreach)
 library(testthat)
 
 # coverage for multi-threading might not be possible (?)
-if (nzchar(Sys.getenv("R_COVR"))) RcppParallel::setThreadOptions(1L)
+if (nzchar(Sys.getenv("R_COVR"))) #RcppParallel::setThreadOptions(1L)
 # old reporter for CMD checks
 options(testthat.default_reporter = "summary")
 
diff --git a/tests/testthat/test-01-unit-02-misc.R b/tests/testthat/test-01-unit-02-misc.R
index 956e491..90ecedf 100644
--- a/tests/testthat/test-01-unit-02-misc.R
+++ b/tests/testthat/test-01-unit-02-misc.R
@@ -110,7 +110,7 @@ test_that("%op% catches errors as expected.", {
     skip_if(foreach::getDoParWorkers() %% 2L == 1L, "sequential case")
     prev_threads <- foreach(dummy = 1L:foreach::getDoParWorkers()) %dopar% {
         nthreads <- as.integer(Sys.getenv("RCPP_PARALLEL_NUM_THREADS", "1"))
-        RcppParallel::setThreadOptions()
+        #RcppParallel::setThreadOptions()
         nthreads
     }
     expect_error(
@@ -125,7 +125,7 @@ test_that("%op% catches errors as expected.", {
         expect_false(nzchar(nthreads_env_var))
     })
     foreach(nthreads = prev_threads) %dopar% {
-        RcppParallel::setThreadOptions(nthreads)
+        #RcppParallel::setThreadOptions(nthreads)
         NULL
     }
 })
diff --git a/tests/testthat/test-06-parallel.R b/tests/testthat/test-06-parallel.R
index d1b645d..2c81b34 100644
--- a/tests/testthat/test-06-parallel.R
+++ b/tests/testthat/test-06-parallel.R
@@ -27,7 +27,7 @@ test_that("Parallel computation gives the same results as sequential", {
     invisible(clusterEvalQ(cl, {
         library(dtwclust)
         # environment variables get inherited by the workers when they are created, so set this
-        if (!nzchar(Sys.getenv("R_COVR"))) RcppParallel::setThreadOptions(num_workers)
+        if (!nzchar(Sys.getenv("R_COVR"))) #RcppParallel::setThreadOptions(num_workers)
         # to test that other RNGkinds won't affect
         RNGkind("default")
         # for regression tests' comparisons
diff --git a/timing-experiments/00-main.R b/timing-experiments/00-main.R
index 0d3540b..c17a908 100644
--- a/timing-experiments/00-main.R
+++ b/timing-experiments/00-main.R
@@ -19,7 +19,7 @@ tic <- proc.time()
 if (file.exists("read-csv.RData")) load("read-csv.RData") else source("10-read-csv.R")
 if (file.exists("dist-results.RData")) load("dist-results.RData") else source("20-distance-experiments.R")
 if (file.exists("cent-results.RData")) load("cent-results.RData") else source("30-prototyping-experiments.R")
-RcppParallel::setThreadOptions(4L)
+#RcppParallel::setThreadOptions(4L)
 if (file.exists("tadpole-results.RData")) load("tadpole-results.RData") else source("40-tadpole-experiments.R")
 if (file.exists("partitional-results.RData")) load("partitional-results.RData") else source("50-partitional-experiments.R")
 toc <- proc.time() - tic
diff --git a/timing-experiments/20-distance-experiments.R b/timing-experiments/20-distance-experiments.R
index 9933a76..c30a3dd 100644
--- a/timing-experiments/20-distance-experiments.R
+++ b/timing-experiments/20-distance-experiments.R
@@ -500,7 +500,7 @@ dist_lbk_multiple <- dplyr::bind_rows(lapply(num_threads_to_test, function(num_t
     mycat("\t\t")
 
     # Set number of threads to use
-    RcppParallel::setThreadOptions(num_threads)
+    
 
     # Loop along series
     benchmarks <- lapply(series, function(this_series) {
@@ -542,7 +542,7 @@ dist_lbk_multiple <- dplyr::bind_rows(lapply(num_threads_to_test, function(num_t
 mycat("\tRunning lb_improved experiments for multiple series\n")
 dist_lbi_multiple <- dplyr::bind_rows(lapply(num_threads_to_test, function(num_threads) {
     mycat("\t\t")
-    RcppParallel::setThreadOptions(num_threads)
+    
 
     benchmarks <- lapply(series, function(this_series) {
         expressions <- lapply(1L:nrow(id_series), function(i) {
@@ -582,7 +582,7 @@ dist_lbi_multiple <- dplyr::bind_rows(lapply(num_threads_to_test, function(num_t
 mycat("\tRunning dtw_lb experiments for multiple series\n")
 dist_dtwlb_multiple <- dplyr::bind_rows(lapply(num_threads_to_test, function(num_threads) {
     mycat("\t\t")
-    RcppParallel::setThreadOptions(num_threads)
+    
 
     id_series <- rbind(
         expand.grid(seq(from = 10L, to = 50L, by = 10L), 10L),
@@ -627,7 +627,7 @@ dist_dtwlb_multiple <- dplyr::bind_rows(lapply(num_threads_to_test, function(num
 mycat("\tRunning sbd experiments for multiple series\n")
 dist_sbd_multiple <- dplyr::bind_rows(lapply(num_threads_to_test, function(num_threads) {
     mycat("\t\t")
-    RcppParallel::setThreadOptions(num_threads)
+    
 
     benchmarks <- lapply(series, function(this_series) {
         expressions <- lapply(1L:nrow(id_series), function(i) {
@@ -662,7 +662,7 @@ dist_sbd_multiple <- dplyr::bind_rows(lapply(num_threads_to_test, function(num_t
 mycat("\tRunning dtw experiments for multiple univariate series\n")
 dist_dtw_univariate_multiple <- dplyr::bind_rows(lapply(num_threads_to_test, function(num_threads) {
     mycat("\t\t")
-    RcppParallel::setThreadOptions(num_threads)
+    
 
     benchmarks <- lapply(series, function(this_series) {
         expressions <- lapply(window_sizes, function(window_size) {
@@ -702,7 +702,7 @@ dist_dtw_univariate_multiple <- dplyr::bind_rows(lapply(num_threads_to_test, fun
 mycat("\tRunning dtw experiments for multiple multivariate series\n")
 dist_dtw_multivariate_multiple <- dplyr::bind_rows(lapply(num_threads_to_test, function(num_threads) {
     mycat("\t\t")
-    RcppParallel::setThreadOptions(num_threads)
+    
 
     benchmarks <- lapply(series_mv, function(this_series) {
         expressions <- lapply(window_sizes, function(window_size) {
@@ -755,7 +755,7 @@ id_series <- id_series[order(id_series[,1L] * id_series[,2L]),]
 mycat("\tRunning sdtw experiments for multiple univariate series\n")
 dist_sdtw_univariate_multiple <- dplyr::bind_rows(lapply(num_threads_to_test, function(num_threads) {
     mycat("\t\t")
-    RcppParallel::setThreadOptions(num_threads)
+    
 
     benchmarks <- lapply(series, function(this_series) {
         expressions <- lapply(1L:nrow(id_series), function(i) {
@@ -790,7 +790,7 @@ dist_sdtw_univariate_multiple <- dplyr::bind_rows(lapply(num_threads_to_test, fu
 mycat("\tRunning normalized_gak experiments for multiple univariate series\n")
 dist_ngak_univariate_multiple <- dplyr::bind_rows(lapply(num_threads_to_test, function(num_threads) {
     mycat("\t\t")
-    RcppParallel::setThreadOptions(num_threads)
+    
 
     benchmarks <- lapply(series, function(this_series) {
         expressions <- lapply(window_sizes, function(window_size) {
diff --git a/timing-experiments/30-prototyping-experiments.R b/timing-experiments/30-prototyping-experiments.R
index 6829490..d22113f 100644
--- a/timing-experiments/30-prototyping-experiments.R
+++ b/timing-experiments/30-prototyping-experiments.R
@@ -137,7 +137,7 @@ cent_dba_univariate <- with(
         # Loop across number of threads to test
         dplyr::bind_rows(lapply(num_threads, function(num_threads) {
             mycat("\t\t")
-            RcppParallel::setThreadOptions(num_threads)
+            
 
             # Loop along extracted subsets
             benchmarks <- lapply(series, function(this_series) {
@@ -184,7 +184,7 @@ cent_dba_multivariate_byS <- with(
         # Loop across number of threads to test
         dplyr::bind_rows(lapply(num_threads, function(num_threads) {
             mycat("\t\t")
-            RcppParallel::setThreadOptions(num_threads)
+            
 
             # Loop along extracted subsets
             benchmarks <- lapply(series_mv, function(this_series) {
@@ -232,7 +232,7 @@ cent_dba_multivariate_byV <- with(
         # Loop across number of threads to test
         dplyr::bind_rows(lapply(num_threads, function(num_threads) {
             mycat("\t\t")
-            RcppParallel::setThreadOptions(num_threads)
+            
 
             # Loop along extracted subsets
             benchmarks <- lapply(series_mv, function(this_series) {
@@ -280,7 +280,7 @@ cent_sdtw_univariate <- with(
         # Loop across number of threads to test
         dplyr::bind_rows(lapply(num_threads, function(num_threads) {
             mycat("\t\t")
-            RcppParallel::setThreadOptions(num_threads)
+            
 
             # Loop along subset of extracted subsets
             series <- series[seq(from = 1L, to = length(series), by = 2L)]
@@ -324,7 +324,7 @@ cent_sdtw_multivariate <- with(
         # Loop across number of threads to test
         dplyr::bind_rows(lapply(num_threads, function(num_threads) {
             mycat("\t\t")
-            RcppParallel::setThreadOptions(num_threads)
+            
 
             # Loop along subset of extracted subsets
             series_mv <- series_mv[seq(from = 1L, to = length(series_mv), by = 2L)]
diff --git a/vignettes/sarda.R b/vignettes/sarda.R
index 71e953e..d275266 100644
--- a/vignettes/sarda.R
+++ b/vignettes/sarda.R
@@ -233,7 +233,7 @@ ggplot(dtw_comparison,
     labs(x = "Series' length", y = expression("Median time ("*mu*"s)"))
 
 ## ----lbk-multiple--------------------------------------------------------
-RcppParallel::setThreadOptions(1L)
+#RcppParallel::setThreadOptions(1L)
 series <- reinterpolate(CharTraj, 100L)
 window_size <- 30L
 id_series <- cbind(c(seq(from = 10L, to = 40L, by = 10L),
@@ -370,7 +370,7 @@ ggplot(dtw_comparison,
     facet_wrap(~ distance, scales = "free_y") +
     labs(x = "Total number of distance calculations", y = "Median time (s)")
 
-RcppParallel::setThreadOptions()
+#RcppParallel::setThreadOptions()
 
 ## ----sbd-alignment, out.width = "0.45\\linewidth", fig.width = 6, fig.cap = "Visualization of the NCCc-based alignment performed on two sample series. After alignment, the second (red) series is either truncated and/or prepended/appended with zeros so that its length matches the first(black) series.", fig.subcap = c("Series before alignment", "Series after alignment")----
 matplot(cbind(CharTraj[[61L]], CharTraj[[65L]]),
