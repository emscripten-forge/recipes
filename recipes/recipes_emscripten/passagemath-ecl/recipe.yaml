context:
  version: 10.8.12

package:
  name: passagemath-ecl
  version: ${{ version }}

source:
- url: https://pypi.org/packages/source/p/passagemath-ecl/passagemath_ecl-${{ version }}.tar.gz
  sha256: f122e71c3a226d529a9c6a4c054482544c3df0f0cd7e5568a6e56f6acae63a7e

build:
  script: |
    export ECL_CONFIG="${PREFIX}/bin/ecl-config"
    sed -i.bak "s/'gsl', 'ecl'/'ecl',/" setup.py
    ${{ PYTHON }} -m pip install .
  number: 0

requirements:
  build:
  - python
  - crossenv >=1.2
  - cross-python_emscripten-wasm32
  - ${{ compiler("c") }}
  - ${{ compiler("cxx") }}
  - pip
  - passagemath-setup
  - passagemath-environment
  - cython<3.3
  - cysignals
  - pkgconfig
  - passagemath-abi == ${{ version }}
  host:
  - python
  - ecl
  - gmp
  - libgc
  - passagemath-categories
  run:
  - python
  - cysignals
  - passagemath-categories
  - passagemath-environment

tests:
- script: pytester
  files:
    recipe:
    - test_passagemath_ecl.py
  requirements:
    build:
    - pytester
    run:
    - pytester-run
    - passagemath-repl

about:
  license: GPL-2.0-or-later
  license_file: README.rst
  summary: 'passagemath: Embeddable Common Lisp'
  description: |
    Distribution of a part of the Sage library.
    It provides the Python and Cython interfaces to Embeddable Common Lisp.

extra:
  emscripten_tests:
    python:
      pytest_files:
      - test_passagemath_ecl.py
  recipe-maintainers:
  - mkoeppe
