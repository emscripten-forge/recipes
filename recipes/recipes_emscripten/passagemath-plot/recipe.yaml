context:
  version: 10.8.12

package:
  name: passagemath-plot
  version: ${{ version }}

source:
- url: https://pypi.org/packages/source/p/passagemath-plot/passagemath_plot-${{ version }}.tar.gz
  sha256: 724ea093ee923a4f3bdd5b8dc8a4ff18e8125093ee98af9e451c1c67b8265bac

build:
  script: ${{ PYTHON }} -m pip install .
  number: 0

requirements:
  build:
  - python
  - crossenv >=1.2
  - cross-python_emscripten-wasm32
  - ${{ compiler("c") }}
  - ${{ compiler("cxx") }}
  - pip
  - passagemath-setup
  - passagemath-environment
  - cython<3.3
  - gmpy2
  - numpy
  - cysignals
  - memory-allocator
  - pkgconfig
  - numpy
  - passagemath-abi == ${{ version }}
  host:
  - python
  - gmp
  - gsl
  - numpy
  - passagemath-modules
  run:
  - python
  - cysignals
  - gmpy2
  - matplotlib
  - memory-allocator
  - numpy
  - pillow
  - passagemath-modules
  - scipy

tests:
- script: pytester
  files:
    recipe:
    - test_passagemath_plot.py
  requirements:
    build:
    - pytester
    run:
    - pytester-run
    - passagemath-repl

about:
  license: GPL-2.0-or-later
  license_file: README.rst
  summary: 'passagemath: Plotting and graphics with Matplotlib, Three.JS, etc.'
  description: |
    Distribution of a small part of the Sage library.
    It provides the namespace packages sage.plot and sage.plot.plot3d, which provide
    functions for plotting that are very similar to Mathematica's plotting functions.

extra:
  emscripten_tests:
    python:
      pytest_files:
      - test_passagemath_plot.py
  recipe-maintainers:
  - mkoeppe
