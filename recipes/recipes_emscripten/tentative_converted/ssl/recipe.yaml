package:
  name: ssl
  version: 1.0.0
source:
- path: empty
build:
  sharedlibrary: true
requirements:
  run:
  - openssl
  build:
  - '{{ compiler("c") }}'
extra:
  emscripten_tests:
    python:
      pytest_files:
      - test_import_ssl.py
