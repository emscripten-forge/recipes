context:
  version: 1.10.0

package:
  name: showcase
  version: '{{ version }}'

source:
  path: /home/derthorsten/src/showcase/
  # url: https://github.com/DerThorsten/showcase/archive/refs/tags/{{ version }}.tar.gz
  # sha256: 70d9cbda62fcf7d205b72fc499c9ec82082b9cbffa8ffb84be86223e7b3a71d0

build:
  number: 0
  # run_exports:
  #   - '{{ pin_subpackage("showcase", max_pin="x.x") }}'

requirements:
  build:
    - '{{ compiler("c") }}'
    - '{{ compiler("cxx") }}'
    - cmake
    - ninja

  host:
    - mypython
    - pybind11
    - sel(emscripten): bzip2
    - sel(emscripten): zlib
    # - sel(emscripten): mpdecimal


about:
  home: https://github.com/DerThorsten/showcase
  license: MIT
  license_family: MIT
  # license_file: LICENSE
  summary: showcase is a toy lib
  doc_url: https://github.com/DerThorsten/showcase/
  dev_url: https://github.com/DerThorsten/showcase

extra:
  recipe-maintainers:
    - DerThorsten

