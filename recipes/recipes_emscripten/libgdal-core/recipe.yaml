context:
  name: libgdal-core
  version: 3.12.3

source:
  url: https://github.com/OSGeo/gdal/archive/refs/tags/v${{ version }}.tar.gz
  sha256: 31861d83eeb16dac6ed872370cb9f86db19de1cb48a93e3847212cdf24794ead
  patches:
  - patches/0001-Add-CPL_CPUID-guards.patch

build:
  number: 0

outputs:
- package:
    name: ${{ name }}
    version: ${{ version }}
  build:
    script: build.sh
  requirements:
    build:
    - ${{ compiler('c') }}
    - cmake
    - pkg-config
    host:
    - libtiff
    - proj-static
    - libiconv
    - geos-static
    - libpng
  tests:
  - package_contents:
      files:
      - lib/libgdal.so

- package:
    name: ${{ name }}-static
    version: ${{ version }}
  build:
    script: build_static.sh
  requirements:
    build:
    - ${{ compiler('c') }}
    - cmake
    - pkg-config
    host:
    - libtiff
    - proj-static
    - libiconv
    - geos-static
    - libpng
  tests:
  - package_contents:
      files:
      - lib/libgdal.a

about:
  homepage: https://gdal.org/
  license: MIT
  license_file: LICENSE.TXT

extra:
  recipe-maintainers:
  - DerThorsten
  - IsabelParedes
