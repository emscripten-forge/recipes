diff --git a/mlir/python/mlir/runtime/np_to_memref.py b/mlir/python/mlir/runtime/np_to_memref.py
index d65ba51afdb9..a463afde844d 100644
--- a/mlir/python/mlir/runtime/np_to_memref.py
+++ b/mlir/python/mlir/runtime/np_to_memref.py
@@ -99,29 +99,48 @@ def to_numpy(array):
     return array
 
 
-def make_nd_memref_descriptor(rank, dtype):
+def _index_ctype(index_bitwidth=None):
+    """Returns the integer type used by a lowered memref descriptor.
+
+    The target-native index width is used by default. An explicit width may
+    be supplied for a lowering pipeline that overrides ``index-bitwidth``.
+    """
+    if index_bitwidth is None:
+        index_bitwidth = ctypes.sizeof(ctypes.c_void_p) * 8
+    if index_bitwidth == 32:
+        return ctypes.c_int32
+    if index_bitwidth == 64:
+        return ctypes.c_int64
+    raise ValueError(f"unsupported index_bitwidth: {index_bitwidth!r} (expected 32 or 64)")
+
+
+def make_nd_memref_descriptor(rank, dtype, index_bitwidth=None):
+    index_ctype = _index_ctype(index_bitwidth)
+
     class MemRefDescriptor(ctypes.Structure):
         """Builds an empty descriptor for the given rank/dtype, where rank>0."""
 
         _fields_ = [
-            ("allocated", ctypes.c_longlong),
+            ("allocated", ctypes.POINTER(dtype)),
             ("aligned", ctypes.POINTER(dtype)),
-            ("offset", ctypes.c_longlong),
-            ("shape", ctypes.c_longlong * rank),
-            ("strides", ctypes.c_longlong * rank),
+            ("offset", index_ctype),
+            ("shape", index_ctype * rank),
+            ("strides", index_ctype * rank),
         ]
 
     return MemRefDescriptor
 
 
-def make_zero_d_memref_descriptor(dtype):
+def make_zero_d_memref_descriptor(dtype, index_bitwidth=None):
+    index_ctype = _index_ctype(index_bitwidth)
+
     class MemRefDescriptor(ctypes.Structure):
         """Builds an empty descriptor for the given dtype, where rank=0."""
 
         _fields_ = [
-            ("allocated", ctypes.c_longlong),
+            ("allocated", ctypes.POINTER(dtype)),
             ("aligned", ctypes.POINTER(dtype)),
-            ("offset", ctypes.c_longlong),
+            ("offset", index_ctype),
         ]
 
     return MemRefDescriptor
@@ -130,37 +149,47 @@ def make_zero_d_memref_descriptor(dtype):
 class UnrankedMemRefDescriptor(ctypes.Structure):
     """Creates a ctype struct for memref descriptor"""
 
-    _fields_ = [("rank", ctypes.c_longlong), ("descriptor", ctypes.c_void_p)]
+    _fields_ = [("rank", _index_ctype()), ("descriptor", ctypes.c_void_p)]
 
 
-def get_ranked_memref_descriptor(nparray):
-    """Returns a ranked memref descriptor for the given numpy array."""
+def get_ranked_memref_descriptor(nparray, index_bitwidth=None):
+    """Returns a ranked memref descriptor for the given numpy array.
+
+    ``index_bitwidth`` must match the value used when lowering the consuming
+    MLIR module. It defaults to the target's native pointer width.
+    """
+    index_ctype = _index_ctype(index_bitwidth)
     ctp = as_ctype(nparray.dtype)
     if nparray.ndim == 0:
-        x = make_zero_d_memref_descriptor(ctp)()
-        x.allocated = nparray.ctypes.data
+        x = make_zero_d_memref_descriptor(ctp, index_bitwidth)()
+        x.allocated = nparray.ctypes.data_as(ctypes.POINTER(ctp))
         x.aligned = nparray.ctypes.data_as(ctypes.POINTER(ctp))
-        x.offset = ctypes.c_longlong(0)
+        x.offset = index_ctype(0)
         return x
 
-    x = make_nd_memref_descriptor(nparray.ndim, ctp)()
-    x.allocated = nparray.ctypes.data
+    x = make_nd_memref_descriptor(nparray.ndim, ctp, index_bitwidth)()
+    x.allocated = nparray.ctypes.data_as(ctypes.POINTER(ctp))
     x.aligned = nparray.ctypes.data_as(ctypes.POINTER(ctp))
-    x.offset = ctypes.c_longlong(0)
+    x.offset = index_ctype(0)
     x.shape = nparray.ctypes.shape
 
     # Numpy uses byte quantities to express strides, MLIR OTOH uses the
     # torch abstraction which specifies strides in terms of elements.
-    strides_ctype_t = ctypes.c_longlong * nparray.ndim
+    strides_ctype_t = index_ctype * nparray.ndim
     x.strides = strides_ctype_t(*[x // nparray.itemsize for x in nparray.strides])
     return x
 
 
-def get_unranked_memref_descriptor(nparray):
+def get_unranked_memref_descriptor(nparray, index_bitwidth=None):
     """Returns a generic/unranked memref descriptor for the given numpy array."""
-    d = UnrankedMemRefDescriptor()
+    index_ctype = _index_ctype(index_bitwidth)
+
+    class Descriptor(ctypes.Structure):
+        _fields_ = [("rank", index_ctype), ("descriptor", ctypes.c_void_p)]
+
+    d = Descriptor()
     d.rank = nparray.ndim
-    x = get_ranked_memref_descriptor(nparray)
+    x = get_ranked_memref_descriptor(nparray, index_bitwidth)
     d.descriptor = ctypes.cast(ctypes.pointer(x), ctypes.c_void_p)
     return d
 
@@ -174,10 +203,12 @@ def move_aligned_ptr_by_offset(aligned_ptr, offset):
     return content_ptr
 
 
-def unranked_memref_to_numpy(unranked_memref, np_dtype):
+def unranked_memref_to_numpy(unranked_memref, np_dtype, index_bitwidth=None):
     """Converts unranked memrefs to numpy arrays."""
     ctp = as_ctype(np_dtype)
-    descriptor = make_nd_memref_descriptor(unranked_memref[0].rank, ctp)
+    descriptor = make_nd_memref_descriptor(
+        unranked_memref[0].rank, ctp, index_bitwidth
+    )
     val = ctypes.cast(unranked_memref[0].descriptor, ctypes.POINTER(descriptor))
     content_ptr = move_aligned_ptr_by_offset(val[0].aligned, val[0].offset)
     np_arr = np.ctypeslib.as_array(content_ptr, shape=val[0].shape)
