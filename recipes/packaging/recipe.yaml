context:
  version: '21.3'

package:
  name: packaging
  version: '{{ version }}'

source:
- sha256: dd47c42927d89ab911e606518907cc2d3a1f38bbd026385970643f9c5b8ecfeb
  url: https://pypi.io/packages/source/p/packaging/packaging-{{ version }}.tar.gz

build:
  number: 0

requirements:
  build:
    - cross-python_emscripten-32
    - pip
  host:
    - python
  run:
    - pyparsing

extra:
  emscripten_tests:
    python:
      pytest_files:
      - test_import_packaging.py
