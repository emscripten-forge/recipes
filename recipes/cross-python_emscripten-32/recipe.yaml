context:
  name: cross-python_emscripten-32
  version: 3.10

package:
  name: '{{ name|lower }}'
  version: '{{ version }}'

build:
  number: 0


requirements:
  run:
    - crossenv >=1.0
    - rsync
    - sed
    - python 3.10
    - setuptools
    - emboa
about:
  home: cross-python


extra:
  recipe-maintainers:
    - DerThorsten
