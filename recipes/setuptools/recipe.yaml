context:
  name: setuptools
  version: "1.0.0"

package:
  name: '{{ name|lower }}'
  version: '{{ version }}'

build:
  number: 0
  # string: "0_cp310"

about:
  home: setuptools
  license: BSD-3-Clause
  license_family: BSD
  license_file: LICENSE
  summary: setuptools
  description: setuptools
  doc_url: setuptools
  dev_url: setuptools

extra:
  recipe-maintainers:
    - DerThorsten
