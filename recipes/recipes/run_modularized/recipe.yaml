context:
  name: run_modularized
  version: "0.1.2"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

build:
  number: 2

requirements:
  run:
    - nodejs

about:
  license: BSD-3-Clause
  summary: run_modularized
  description: run_modularized
extra:
  recipe-maintainers:
    - DerThorsten
