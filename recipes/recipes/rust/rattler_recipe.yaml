context:
  name: rust
  version: "1.79.0"

package:
  name: ${{ name|lower }}
  version: ${{ version }}


build:
  number: 30

requirements:
  build:
    - curl

about:
  summary: rust

extra:
  recipe-maintainers:
    - DerThorsten
