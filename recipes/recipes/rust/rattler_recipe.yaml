context:
  name: rust
  version: "1.77.2"

package:
  name: ${{ name|lower }}
  version: ${{ version }}


build:
  number: 15

requirements:
  build:
    - curl

about:
  summary: rust

extra:
  recipe-maintainers:
    - DerThorsten
