context:
  name: rust
  version: 1.86.0

package:
  name: ${{ name }}
  version: ${{ version }}

build:
  number: 1

requirements:
  build:
    - curl

about:
  summary: rust

extra:
  recipe-maintainers:
    - DerThorsten
