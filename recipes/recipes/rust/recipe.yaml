context:
  name: rust
  version: "1.86.0"

package:
  name: ${{ name|lower }}
  version: ${{ version }}


build:
  number: 0

requirements:
  build:
    - curl

about:
  summary: rust

extra:
  recipe-maintainers:
    - DerThorsten
