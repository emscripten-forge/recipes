context:
  version: '1.0.11'

source:
  - path: bin
    target_directory: bin
  - path: pytester
    target_directory: pytester

outputs:
  - package:
      name: pytester
      version: ${{ version }}

    build:
      number: 1
      noarch: python

    requirements:
      host:
        - python
      run:
        - pyjs_code_runner
        - playwright


  - package:
      name: pytester-run
      version: ${{ version }}
    build:
      number: 0
      noarch: generic
    requirements:
      run:
        - pyjs < 2.0
        - pytest
 

about:
  license: MIT
  license_file: LICENSE.txt
  summary: test meta pkg

