context:
  name: only_py_tests
  version: "1.0.0"

package:
  name: '{{ name|lower }}'
  version: '{{ version }}'


build:
  number: 1

requirements:
  run:
    - python 3.11
    - pandas
    - arrow-python
    - matplotlib
    - pandas
    - matplotlib
    - scipy
    - ipython
    - arrow-python
about:
  home: only_py_tests

extra:
  recipe-maintainers:
    - DerThorsten
